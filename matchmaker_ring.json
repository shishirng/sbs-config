{
       "cinder-api": [
             "{{ cinder_api_hosts|map(attribute='hostname')|join(',') }}"
               ],
       "cinder-volume": [
             "{{ cinder_volume_hosts|map(attribute='hostname')|join(',') }}"
               ],
       "cinder-scheduler": [
             "{{ cinder_scheduler_hosts|map(attribute='hostname')|join(',') }}"
               ]
}
